import React from "react";
import CartComponent from "../components/CartComponent";

const CartPage = () => {
  return (
    <div>
      <CartComponent />
    </div>
  );
};

export default CartPage;
